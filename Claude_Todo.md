# Claude Todo

## Offen / spaeter
- [ ] Web-Push-Benachrichtigungen aktivieren (Schritt 2): VAPID-Keys, `web-push`-Library,
      Opt-in-Button (Permission + `pushManager.subscribe`), Speichern in `push_subscriptions`,
      Versand beim Erstellen einer In-App-Notification, `push`/`notificationclick` im Service Worker.
